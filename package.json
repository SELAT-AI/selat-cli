{
  "name": "@selat-ai/selat-cli",
  "version": "0.1.0",
  "description": "Setup helper and runner for SELAT agent payments. Checks Circle CLI, selat-pay, and config without self-installing dependencies.",
  "type": "module",
  "bin": {
    "selat": "./bin/selat.mjs"
  },
  "scripts": {
    "check": "node --check bin/selat.mjs && node --check lib/commands/*.mjs && node --check lib/*.mjs",
    "start": "node bin/selat.mjs"
  },
  "engines": {
    "node": ">=18"
  },
  "files": [
    "bin",
    "lib",
    "README.md"
  ],
  "keywords": [
    "selat",
    "selat-cli",
    "x402",
    "circle",
    "agent-payments",
    "cli"
  ],
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/SELAT-AI/selat-cli.git"
  }
}
