{
  "name": "@selat-ai/selat-cli",
  "version": "0.5.6",
  "description": "Setup helper and runner for SELAT agent payments. Bundles the selat-agent-payments skill and selat-pay as dependencies; checks Circle CLI and config separately.",
  "type": "module",
  "bin": {
    "selat": "./bin/selat.mjs"
  },
  "scripts": {
    "check": "for f in bin/selat.mjs lib/*.mjs lib/commands/*.mjs; do node --check \"$f\" || exit 1; done",
    "test": "node --test test/*.test.mjs",
    "start": "node bin/selat.mjs"
  },
  "dependencies": {
    "@selat-ai/selat-agent-payments": "^0.6.1",
    "@selat-ai/selat-pay": "^0.4.1"
  },
  "publishConfig": {
    "access": "public"
  },
  "engines": {
    "node": ">=18"
  },
  "files": [
    "bin",
    "lib",
    "README.md"
  ],
  "keywords": [
    "selat",
    "selat-cli",
    "x402",
    "circle",
    "agent-payments",
    "cli"
  ],
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/SELAT-AI/selat-cli.git"
  }
}
